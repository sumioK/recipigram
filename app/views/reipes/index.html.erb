<% @reipes.each do |reipe| %>
    <%= attachment_image_tag reipe, :image %>
    <%= attachment_image_tag reipe.user, :profile_image, fallback: "no-image.png" %>
    <%= reipe.user.username %>
    <%= reipe.updated_at.strftime("%Y-%m-%d %H:%M") %>

<% end %>